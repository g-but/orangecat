Environment Backups
====================

- This directory stores automatic backups of `.env.local`.
- Do not delete this directory or its contents.
- Use `node scripts/utils/env-manager.js list|backup|restore|validate` to manage env safely.
