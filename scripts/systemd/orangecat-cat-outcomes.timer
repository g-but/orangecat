# SSOT: repo scripts/systemd/ — installed by deploy-selfhost.sh.
# Runs after the 04:30 judgment eval so the two nightly gates don't overlap.
[Unit]
Description=OrangeCat nightly Cat outcome gate (04:45 UTC)

[Timer]
OnCalendar=*-*-* 04:45:00 UTC
Persistent=true

[Install]
WantedBy=timers.target
