Recommended SQL files:

- best_practice_indexes.sql: Non-destructive CREATE INDEX statements to optimize common queries.
- rls_policies.sql: Idempotent RLS enablement + owner-scoped policies (adjust as needed).

Run with psql / Supabase SQL editor in production after review.

